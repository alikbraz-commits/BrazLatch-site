---
description: Run the pre-launch audit (brand, accessibility, SEO) and produce a structured punch list.
---

Invoke the `brand-and-audit` subagent with:

> Run the full pre-launch audit on `index.html` and `thanks.html`. Produce
> the punch list in the format defined in your agent definition (▶ BRAND &
> COPY / ▶ ACCESSIBILITY / ▶ SEO, with [BLOCKER] / [FIX] / [POLISH]
> severities). After the list, propose the handoff plan: which items you
> can self-apply (text-only), which need site-builder, and which need
> Alik's input.

Present the agent's output verbatim. Do not start applying fixes — wait
for Alik to say "go" or to pick specific items.
